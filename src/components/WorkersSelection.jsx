import { useState } from 'react';

const WorkersSelection = () => {
  const [checkedValues, setCheckedValues] = useState([]);

  return <div>WorkersSelection</div>;
};

export default WorkersSelection;
